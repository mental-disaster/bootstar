spring:
  datasource:
    driver-class-name: DB명
    url: DBurl
    username: 사용자계정
    password: 비밀번호

  flyway:
    baseline-on-migrate: true

  jpa:
    hibernate:
      use-new-id-generator-mappings: false

mybatis:
  mapper-locations: mapper/**/*.xml


fileUrl:
  imgUrl: 이미지파일 url